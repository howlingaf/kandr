Working through *The C Programming Language* (2nd edition) by Brian Kernighan
and Denise Richards.

```
make                 # build every program
make run FILE=main   # build and run bin/main
./watcher.sh         # rebuild on save, run the program named by RUN=
```
